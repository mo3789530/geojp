{
  "name": "app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir ./dist --target bun"
  },
  "dependencies": {
    "hono": "^4.11.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}